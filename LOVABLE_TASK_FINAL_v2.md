# 🎨 Задание для lovable.dev: Улучшение визуализации и цветов

## 📊 КРИТИЧЕСКИЕ ПРОБЛЕМЫ С UX/UI

Пользователь не может использовать систему из-за трёх критических проблем:

### 1. ❌ Круговые диаграммы - все сегменты ОДНОГО цвета (серые)
**Проблема:** Невозможно различить приложения на диаграмме.  
**Решение:** Раскрасить сегменты по категориям приложений.

### 2. ❌ Временная шкала активности - цвета сливаются (нечитаемая)
**Проблема:** Невозможно различить периоды активности.  
**Решение:** Пересмотреть палитру с учетом контрастности (WCAG стандарт).

### 3. ❌ Нет объяснений метрик - пользователь не понимает что значат цифры
**Проблема:** Что такое "Productivity Score"? Как считается "Активное время"?  
**Решение:** Добавить tooltips с пояснениями всех метрик.

---

## 🎯 ЗАДАЧИ ДЛЯ ДИЗАЙНЕРА

### ✅ Задача 1: Цветовая схема по категориям приложений

**Backend уже отдаёт категорию в каждом приложении:**

```json
{
  "applications": [
    {
      "process_name": "code.exe",
      "window_title": "main.go - Visual Studio Code",
      "duration": 7200,
      "count": 45,
      "category": "productive"  ← Эта категория приходит из backend!
    }
  ]
}
```

**Категории приложений:**
- `productive` - Работа (IDE, Office, Email)
- `unproductive` - Развлечения (YouTube, соц. сети, игры)
- `neutral` - Браузеры и прочее
- `communication` - Мессенджеры (Teams, Slack, Telegram)
- `entertainment` - Развлечения

**Требование:**  
Подберите цветовую схему для каждой категории с учётом:
- UX/UI best practices (зелёный = productive, красный = unproductive - это рекомендация, но окончательное решение за вами)
- Accessibility (WCAG AA контрастность минимум 4.5:1)
- Проверка для людей с дальтонизмом
- Гармония с общей темой интерфейса

**Пример структуры (цвета выбирает дизайнер):**

```typescript
const CATEGORY_COLORS = {
  productive: '#22c55e',     // Пример: зелёный (или другой по выбору дизайнера)
  unproductive: '#ef4444',   // Пример: красный
  neutral: '#94a3b8',        // Пример: серый
  communication: '#3b82f6',  // Пример: синий
  entertainment: '#f59e0b'   // Пример: оранжевый
}
```

**Применение к круговой диаграмме Applications:**

```typescript
const appChartData = applications.map(app => ({
  name: app.process_name.replace('.exe', ''),
  value: app.duration,
  fill: CATEGORY_COLORS[app.category] || CATEGORY_COLORS.neutral
}))
```

---

### ✅ Задача 2: Исправить временную шкалу активности

**Проблема:** Цвета на временной шкале сливаются - невозможно различить разные периоды.

**Требование:**
1. Пересмотреть цветовую палитру временной шкалы
2. Обеспечить контраст между соседними сегментами минимум 4.5:1 (WCAG AA)
3. Рассмотрите два варианта:
   - **Вариант А:** Использовать категории приложений (CATEGORY_COLORS) для окраски сегментов
   - **Вариант Б:** Использовать отдельную палитру для состояний (active/idle/offline)
4. Проверить как выглядит для людей с дальтонизмом

**Рекомендация:**  
Если шкала показывает разные **приложения** - используйте CATEGORY_COLORS.  
Если показывает **состояния** (active/idle) - создайте отдельную палитру с хорошей дифференциацией.

---

### ✅ Задача 3: Добавить круговую диаграмму "Продуктивность"

**Новая диаграмма** которая показывает распределение времени по категориям:

```typescript
const productivityData = [
  { 
    name: 'Продуктивное', 
    value: summary.productive_time,  // Секунды
    fill: CATEGORY_COLORS.productive
  },
  { 
    name: 'Непродуктивное', 
    value: summary.unproductive_time,
    fill: CATEGORY_COLORS.unproductive
  },
  { 
    name: 'Нейтральное', 
    value: summary.neutral_time,
    fill: CATEGORY_COLORS.neutral
  }
]
```

**Где разместить:**  
Рядом с диаграммой Applications или в отдельной карточке Dashboard.

---

### ✅ Задача 4: Добавить пояснения всех метрик

**КРИТИЧНО:** Пользователь не понимает что означают метрики!

Добавить **информационную иконку ℹ️** рядом с каждой метрикой.  
При наведении/клике показывать tooltip с объяснением.

#### **Метрика: Продуктивность (Productivity Score)**

```
💡 Продуктивность — процент времени в рабочих приложениях.

Формула:
(Продуктивное время / Активное время) × 100%

Продуктивные приложения: IDE (Visual Studio Code, PyCharm), 
офисные программы (Word, Excel), почта (Outlook)

Непродуктивные: YouTube, соц. сети, игры

Нейтральные: Браузеры (зависит от контекста использования)
```

#### **Метрика: Активное время (Active Time)**

```
💡 Активное время — когда пользователь работает за компьютером.

Определяется по активности клавиатуры и мыши (Windows API).

Если нет активности >5 минут → время считается как "Простой"
```

#### **Метрика: Простой (Idle Time)**

```
💡 Простой — когда пользователь отошёл от компьютера.

Если нет активности клавиатуры/мыши более 5 минут,
но менее 30 минут → время считается как простой.

Это время НЕ учитывается в расчёте продуктивности.
```

#### **Метрика: Offline**

```
💡 Offline — компьютер не используется.

Если нет активности более 30 минут → пользователь offline.
Или агент не отправляет данные на сервер.
```

**Реализация:**
- Добавить иконку `<InfoIcon />` или ℹ️ рядом с названием метрики
- При наведении показывать `<Tooltip>` с объяснением
- Или создать раздел "❓ Справка" с описанием всех метрик

---

### ✅ Задача 5: KPI "Productivity Score" — визуальный индикатор

**Требование:** Добавить заметный визуальный индикатор продуктивности.

**Варианты реализации:**

**Вариант А: Badge с цветовым индикатором**
```tsx
const productivityScore = (summary.productive_time / summary.total_active_time) * 100

<Badge color={productivityScore > 50 ? 'green' : productivityScore > 30 ? 'yellow' : 'red'}>
  {productivityScore.toFixed(0)}% Продуктивность
</Badge>
```

**Вариант Б: Progress bar**
```tsx
<Progress 
  value={productivityScore} 
  className={productivityScore > 50 ? 'bg-green-500' : 'bg-red-500'}
/>
<p>{productivityScore.toFixed(0)}% Продуктивность</p>
```

**Вариант В: Gauge chart (полукруг)**
```
      50%
     /   \
    /  ✓  \
   /_______\
  Продуктивность
```

**Расположение:** В Summary блоке, Dashboard или в карточке с диаграммами.

---

### ✅ Задача 6: Легенда для диаграмм

**Требование:** Добавить легенду с объяснением категорий.

**Содержание легенды:**
- 🟢 **Productive** — Работа (IDE, Office, Email)
- 🔴 **Unproductive** — Развлечения (YouTube, соц. сети, игры)
- ⚪ **Neutral** — Браузеры и прочее
- 🔵 **Communication** — Мессенджеры (Teams, Slack)
- 🟠 **Entertainment** — Развлечения

**Где разместить:**  
Под круговыми диаграммами или в sidebar.

---

## 📋 Данные от Backend (API Response Reference)

Backend уже отдаёт все необходимые данные:

### 📊 Endpoint: Отчёт дня для пользователя

**Endpoint:** `GET /api/reports/daily/{username}?date=YYYY-MM-DD`

```json
{
  "applications": [
    {
      "process_name": "code.exe",
      "window_title": "main.go - Visual Studio Code",
      "duration": 7200,       // Секунды
      "count": 45,            // Количество сегментов
      "category": "productive"  ← Категория для цвета!
    },
    {
      "process_name": "chrome.exe",
      "window_title": "YouTube - Google Chrome",
      "duration": 1800,
      "count": 12,
      "category": "unproductive"
    }
  ],
  "summary": {
    "total_active_time": 28800,    // 8 часов в секундах
    "total_idle_time": 3600,       // 1 час в секундах
    "productive_time": 14400,      // 4 часа
    "unproductive_time": 1800,     // 30 минут
    "neutral_time": 12600,         // 3.5 часа
    "productivity_score": 50.0     // 50% продуктивности
  }
}
```

**Примечание:** `productivity_score` уже рассчитывается на backend!

---

### 📚 Endpoint: Справочник программ (управление категориями)

Эти endpoints используются для настройки категорий приложений.

#### **GET /api/process-catalog** - Получить список всех программ в справочнике

**Query параметры:**
- `category` (optional) - фильтр по категории
- `search` (optional) - поиск по названию
- `active_only` (optional, default: true) - только активные записи

**Response:**
```json
{
  "data": [
    {
      "id": "uuid-here",
      "process_name": "code.exe",
      "process_pattern": "code*",
      "category": "productive",
      "created_at": "2025-01-15T10:00:00Z",
      "updated_at": "2025-01-15T10:00:00Z",
      "created_by": "admin",
      "updated_by": "admin",
      "is_active": 1
    }
  ],
  "total": 142
}
```

#### **POST /api/process-catalog** - Добавить программу в справочник

**Request body:**
```json
{
  "process_name": "notepad.exe",
  "process_pattern": "notepad*",
  "category": "productive"
}
```

**Response:**
```json
{
  "status": "success",
  "message": "Category created successfully"
}
```

#### **PUT /api/process-catalog/:id** - Обновить категорию программы

**Request body:**
```json
{
  "process_name": "notepad.exe",
  "process_pattern": "notepad*",
  "category": "neutral"
}
```

**Response:**
```json
{
  "status": "success",
  "message": "Category updated successfully"
}
```

#### **DELETE /api/process-catalog/:id** - Удалить программу из справочника

**Response:**
```json
{
  "status": "success",
  "message": "Category deleted successfully"
}
```

---

### 🔄 Важно: Backend URL

**Production:** `http://monitor.net.gslaudit.ru`  
**Все API запросы:** `http://monitor.net.gslaudit.ru/api/...`

---

## ✅ Критерии приёмки

**Минимальные требования для релиза:**

1. ✅ Круговые диаграммы раскрашены по категориям
2. ✅ Временная шкала читаемая, цвета не сливаются
3. ✅ Добавлена диаграмма "Продуктивность" (распределение по категориям)
4. ✅ Есть пояснения для всех метрик (tooltips или раздел "Справка")
5. ✅ KPI "Productivity Score" отображается визуально (badge/progress/gauge)
6. ✅ Есть легенда категорий под диаграммами
7. ✅ Цвета выбраны по рекомендациям UX/UI дизайнера
8. ✅ Соблюдена accessibility (WCAG AA контрастность)

---

## 🎨 Рекомендации для дизайнера

### Цветовая палитра
- Используйте существующую цветовую палитру проекта (если есть)
- Обеспечьте контраст минимум **4.5:1** (WCAG AA)
- Проверьте цвета через симулятор дальтонизма
- Используйте **не только цвет**, но и паттерны/иконки где возможно

### Семантика цветов
- 🟢 Зелёный = Хорошо (productive, высокая продуктивность)
- 🔴 Красный = Плохо (unproductive, низкая продуктивность)
- 🔵 Синий = Нейтральная коммуникация
- ⚪ Серый = Нейтральное использование
- 🟠 Оранжевый = Предупреждение / развлечения

### Accessibility
- Тестируйте на людях с разными типами дальтонизма
- Добавьте текстовые метки где это критично
- Обеспечьте keyboard navigation для tooltips

---

## 🛠 Инструменты для реализации

**UI компоненты:**
- Recharts / Chart.js для диаграмм
- shadcn/ui для Badge, Tooltip, Progress
- Radix UI для accessible компонентов

**Примеры кода:**

```typescript
// Цвета категорий (выбирает дизайнер)
const CATEGORY_COLORS = {
  productive: '#22c55e',
  unproductive: '#ef4444',
  neutral: '#94a3b8',
  communication: '#3b82f6',
  entertainment: '#f59e0b'
}

// Данные для диаграммы Applications
const appChartData = report.applications.map(app => ({
  name: app.process_name.replace('.exe', ''),
  value: app.duration,
  fill: CATEGORY_COLORS[app.category] || CATEGORY_COLORS.neutral
}))

// Данные для диаграммы Productivity
const productivityChartData = [
  { name: 'Productive', value: report.summary.productive_time, fill: CATEGORY_COLORS.productive },
  { name: 'Unproductive', value: report.summary.unproductive_time, fill: CATEGORY_COLORS.unproductive },
  { name: 'Neutral', value: report.summary.neutral_time, fill: CATEGORY_COLORS.neutral }
]

// Productivity Score
const productivityScore = report.summary.productivity_score // Уже посчитано!
```

---

**Итого:** Реализуйте эти изменения с использованием современных UI компонентов и best practices UX/UI дизайна. 🎨

