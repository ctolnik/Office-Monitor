# ✅ Обновлено задание для lovable.dev

## 📝 ЧТО ДОБАВЛЕНО:

В файл `LOVABLE_TASK_FINAL_v2.md` добавлена **секция с endpoints для справочника программ**.

### Новые endpoints в документации:

1. **GET /api/process-catalog** - Получить список программ в справочнике
   - Query параметры: category, search, active_only
   - Возвращает массив категорий с pagination

2. **POST /api/process-catalog** - Добавить программу в справочник
   - Body: { process_name, process_pattern, category }
   - Создаёт новую категорию

3. **PUT /api/process-catalog/:id** - Обновить категорию
   - Body: { process_name, process_pattern, category }
   - Обновляет существующую запись

4. **DELETE /api/process-catalog/:id** - Удалить программу
   - Мягкое удаление (soft delete)

---

## 🎯 ЗАЧЕМ ЭТО НУЖНО:

Фронтенд-разработчик на lovable.dev теперь знает:

1. ✅ Какие endpoints использовать для справочника программ
2. ✅ Какой формат данных отправлять/получать
3. ✅ Как выглядит структура ответа
4. ✅ Какой URL production сервера (`http://monitor.net.gslaudit.ru`)

---

## 📋 ТЕПЕРЬ В ЗАДАНИИ ЕСТЬ:

### Раздел "📋 Данные от Backend (API Response Reference)"

Содержит:
1. **📊 Отчёт дня** - `GET /api/reports/daily/{username}`
2. **📚 Справочник программ** - CRUD операции `/api/process-catalog`
3. **🔄 Backend URL** - Production URL

---

## ✅ РЕЗУЛЬТАТ:

Теперь разработчик на lovable.dev может:
- ✅ Реализовать справочник программ
- ✅ Добавить UI для CRUD операций
- ✅ Интегрироваться с правильными endpoints
- ✅ Не гадать какой URL использовать

