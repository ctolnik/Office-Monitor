# 🚨 СРОЧНО: Как исправить ошибки 404 на Production

## Проблема
Frontend получает **404 Not Found** для новых API endpoints:
- ❌ `GET /api/users`
- ❌ `GET /api/settings/app-categories`  
- ❌ `GET /api/screenshot/{id}`

## Причина
**Production сервер (monitor.net.gslaudit.ru) работает со старой версией кода!**

Код обновлён в Replit, но docker контейнер на production НЕ ПЕРЕЗАПУЩЕН.

## Решение (5 минут)

На production сервере выполните:

```bash
# 1. Перейдите в директорию проекта
cd /path/to/Office-Monitor

# 2. Обновите код из Git
git pull origin main

# 3. Пересоберите server контейнер
docker-compose build server

# 4. Перезапустите сервисы
docker-compose up -d

# 5. Проверьте что заработало
curl http://localhost:8081/api/users
# Должно вернуть: ["user1", "user2", ...]
```

## Проверка

После обновления в браузере:
- ✅ Ошибки 404 исчезнут
- ✅ Dropdown "Выберите пользователя" заполнится
- ✅ Скриншоты загрузятся

## Что было добавлено

**4 новых API endpoint:**

1. `GET /api/users` - список пользователей
2. `GET /api/settings/app-categories` - категории приложений
3. `GET /api/activity/segments` - сегменты активности для timeline
4. `GET /api/screenshot/:id` - alias для загрузки скриншотов

**Итого:** 51 endpoint (было 47)

---

**Важно:** Изменения уже в main ветке Git. Просто сделайте `git pull` и пересоберите контейнер!
